import 'package:flutter/material.dart';

class TabletPhoneLoginPage extends StatelessWidget {
  const TabletPhoneLoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children: [
            Center(
              child: Text("Tablet"),
            )
          ],
        ),
      ),
    );
  }
}
