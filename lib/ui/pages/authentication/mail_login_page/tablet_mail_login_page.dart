import 'package:flutter/material.dart';

class TabletMailLoginPage extends StatelessWidget {
  const TabletMailLoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children: [
            Center(
              child: Text("Tablet"),
            )
          ],
        ),
      ),
    );
  }
}
