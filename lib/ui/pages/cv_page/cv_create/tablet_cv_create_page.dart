import 'package:flutter/material.dart';

class TabletCvCreatePage extends StatelessWidget {
  const TabletCvCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children: [
            Center(
              child: Text("Tablet"),
            )
          ],
        ),
      ),
    );
  }
}
